language: node_js
node_js:
    - '10'
install:
    - npm install
script:
    - npm run deploy
deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    project: ./dist/
    on:
        branch: master
